# Default editor
export EDITOR="vi"

# Default less settings
export LESS="-FRX"

# nvm path
export NVM_DIR=~/.nvm

# Make brew work nicely with nvm
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" # This loads nvm
